{
  "name": "@jevdeck/evaluation",
  "version": "0.1.0",
  "description": "Measures the §5 quality gates against stored generation output. Never imported by production code.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
